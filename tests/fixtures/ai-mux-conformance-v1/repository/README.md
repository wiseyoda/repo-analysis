# Sealed conformance fixture

This repository intentionally contains source, a large ignored candidate, a
symlink declaration, and a dirty-file declaration. The materializer creates
the platform-specific symlink and Git state.
