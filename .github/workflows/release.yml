name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ../../../repostat-${{ matrix.target }}.tar.gz repostat
          cd ../../..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: repostat-${{ matrix.target }}
          path: repostat-${{ matrix.target }}.tar.gz

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            repostat-x86_64-apple-darwin/repostat-x86_64-apple-darwin.tar.gz
            repostat-aarch64-apple-darwin/repostat-aarch64-apple-darwin.tar.gz
            repostat-x86_64-unknown-linux-gnu/repostat-x86_64-unknown-linux-gnu.tar.gz
          generate_release_notes: true
